<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/assets/img/rsz_2dreamina-2026-06-19-4111-proyecto_sin_no.png" type="image/x-icon">
    <link rel="stylesheet" href="/css/styles.css">

    <% if (typeof estilos !== 'undefined') { %>
        <% estilos.forEach(function(css) { %>
            <link rel="stylesheet" href="<%= css %>">
        <% }); %>
    <% } %>

    <title><%=title%></title>
</head>
<body>